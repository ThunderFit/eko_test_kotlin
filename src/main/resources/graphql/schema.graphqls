input CurrencyInput {
    srcCode: ID!
    price: Float!
    dstCodes: [ID!]!
}

type Currency {
    code: String!
    price: Float!
}

type Query {
    "Currency может быть null, если для нее не нашлось курса."
    convert(input: CurrencyInput!): [Currency]!
}
